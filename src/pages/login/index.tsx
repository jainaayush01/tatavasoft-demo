import { Typography } from "@material-ui/core";
import React from "react";

const Login: React.FC = () => {
	return <Typography variant="h5" color="primary">Login Page</Typography>;
};

export default Login;
